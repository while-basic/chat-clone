import SwiftUI

extension Color {
    static let chatBackground = Color.black
    static let messageInputBackground = Color(.systemGray6).opacity(0.2)
    static let suggestionBackground = Color(.systemGray6).opacity(0.3)
    static let textPrimary = Color.white
    static let textSecondary = Color(.systemGray)
} 